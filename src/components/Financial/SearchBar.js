import React from 'react';

const SearchBar = () => (
  <div className="searchbar">
    <input className="searchbar-input" placeholder="Search..." />
    <a className="search-btn">Go</a>
  </div>
);

export default SearchBar;
